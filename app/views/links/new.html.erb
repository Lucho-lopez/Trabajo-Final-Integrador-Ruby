<!DOCTYPE html>
<html>
<head>
  <style>
    body {
      font-family: Arial, sans-serif;
      padding: 20px;
      background-color: #f5f5f5;
    }
    h1 {
      text-align: center;
      color: #333;
      margin-bottom: 20px;
      font-size: 24px;
    }
    #content {
      text-align: center; /* Centra el contenido */
    }
    #back-to-links {
      display: inline-block;
      padding: 8px 20px;
      background-color: #4CAF50;
      color: white;
      border: none;
      border-radius: 4px;
      text-decoration: none;
      transition: background-color 0.3s ease;
      margin-top: 10px;
      box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1); /* Agrega sombra */
    }
    #back-to-links:hover {
      background-color: #45a049;
    }
    #back-to-links span {
      font-weight: bold;
    }
  </style>
</head>
<body>
  <div id="content">
    <h1>Nuevo link</h1>

    <%= render "form", link: @link %>

    <a href="<%= links_path %>" id="back-to-links">
      <span>Volver a links</span>
    </a>
  </div>
</body>
</html>
