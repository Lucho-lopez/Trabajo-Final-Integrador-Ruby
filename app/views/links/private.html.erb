<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Link Privado | ChqTo</title>

  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <%= stylesheet_link_tag "layouts/links", "data-turbo-track": "reload" %>

  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
</head>

<body style="height:100vh; height:100dvh;">
  <main class="flex flex-col content-center h-full">
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>
    <div class="flex flex-col items-center">
      <h1>Ingrese la contraseña para autenticarse</h1>
      <%= form_with(url: request.path, method: :post) do |form| %>
        <div class="field">
          <%= form.label :password, "Contraseña" %>
          <%= form.password_field :password %>
        </div>
        <div class="actions">
          <%= form.submit "Acceder" %>
        </div>
      <% end %>
    </div>
  </main>
</body>
</html>